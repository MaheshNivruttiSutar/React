# 12 — MUI component customization with sx

**Self-check question:** Can you customize an MUI component using the `sx` prop?

## Topics to cover

- The `sx` prop for one-off styling
- Theme-aware values and breakpoints in `sx`
- Overriding nested component styles
- `sx` vs `styled` vs `className`

## Practice

Add your notes and example code in this folder.
