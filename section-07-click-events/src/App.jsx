//Click Events:
//Click Events are a way to handle events when a user clicks on a button or an element.
//It is used to handle events when a user clicks on a button or an element.
//Click Events are a way to handle events when a user clicks on a button or an element.

import Button from './Button.jsx';
import ProfilePicture from './ProfilePicture.jsx';

function App() {
  return (
    <>
      <Button />
      <ProfilePicture />
    </>
  )
}

export default App
